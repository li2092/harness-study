# 第二卷概念与引用核查 + 修复 · 2026-07-25

> 触发：用户要求核对第二卷有无概念性错误、各项引用是否正确。
> 方法：三轮。①审计 22 路 opus（7 路概念分章 + 4 路引用 + 11 路对抗验证，逐维度独立取证后互驳）；②修复 16 路（8 组分文件改稿 + 8 路复核，每组独占文件避免冲突）；③裁决执行 8 路（4 组 + 4 路复核）。跨文件收口与规格同步由主控直接做。
> 性质：审核 + 已落实修复。本文件是编辑工作区留痕，**不随卷出版**。
> 前序：`volume2-audit-20260724.md`（全文线审核批，逻辑连续性与缩写）。本轮不重复其已登记的机械项。

## 一、总判

**确认 47 处、已修 47 处，含 P0 一处。** 驳回假阳性 7 条。

概念主干没有塌方——六契约的划分、状态机、事件溯源、durable execution、权限模型这些骨架经得住查。**问题集中在引用层，而且有方向性：转述几乎一律朝"支持本书论点"的方向偏移，且偏移都落在最需要外部背书的那一格。** 这不是零散笔误，是一类系统性风险，终稿前对剩余【经验】级断言应过同一把尺。

## 二、P0：凭空的字段表

**02-correct-runtime-v1.0.md:67** 引 Harness Handbook（arXiv:2607.13285）称其自动整理出的行为单元字段是「Trigger、Permission rule、State change、Execution path、Edge cases、Evidence」，与契约六要素「高度同构」，作为"为什么恰好六个要素"的旁证。

主控亲拉全文核（HTML 98,200 字符 + PDF）：**`Permission` 0 次、`Evidence` 0 次**（小写 permission 仅 1 次在实验设置段，evidence 10 次全在 BGPD 语境）。论文实有的是附录 D.1.4 的 L3 输出 schema：`locator_role / stage_context / synopsis / interface / execution_flow / design_decisions / relations`。

凭空多出的两格恰是「权限」与「证据」——六要素里最需要外部佐证的两格。已改用实有 schema，"高度同构"降为部分重叠，对不上的格子明写。02:217 引用条目同步。

> 注：子 agent 曾建议改用 Figure 1 卡片字段（Purpose / Triggering Conditions / …），主控核不实（图片型 figure，HTML 与 PDF 均提取不到），**明令不采用**。旁证力度因此减弱，未回补别的说法——若判断论证撑不住，需另找能覆盖 owner／guard／evidence 三格的来源，不能挂在这篇上。

## 三、外部引用（均经一手源逐字核）

| 位置 | 原表述 | 实况 |
|---|---|---|
| 01:13、01:80、02:24、03:141、90:467 | 规范缺陷 41.77%／协调失败 36.94%／验证缺失 21.30%，1600 余条 | **v2 的旧数**。v3（2025-10-26，当前版本）图 1 为 44.2%／32.3%／23.5%，数据集 1642 条；v3 全文 `41.7`/`36.9`/`21.3` **零命中**。全卷已统一 v3 |
| 01:13 | "标注 1600 余条…（Kappa 0.88）" | κ=0.88 属 **150 条**轨迹的建分类法阶段专家标注；1642 条走 LLM 标注管线（与专家 κ=0.77，跨域 0.79）。已分述 |
| 01:13 | "仅补上验证类干预，就能换回约 +15.6 个百分点" | 原文是给 **ChatDev** 加高层任务目标验证、在 **ProgramDev** 上 +15.6%，且是全部干预里的 **max**。限定已补回 |
| 02:136、02:212 | "execution alignment failure…占其故障分类的 36.4%" | 36.4% 归 **Contract / format** 一档；execution-alignment 是 §5.2 的伞形概念，**无占比**；五档症状分母是失败轨迹、**非互斥**。已重写 |
| 02:88、02:210 | "PCS 框架的 sanity check 三件套" | 摘要原文 `a pair of lightweight sanity checks`、`the two checks`——**两件**。被当作第三件的扰动组是两项检验共用的输入。已改为"两项 + 扰动族，本书打包成三件套（打包是本书的工程改写）"。此前与第一卷 `07-harness-lab.md:49` 自相矛盾，现已自洽 |
| 02:92 | τ-bench"金额 1628 美元没在回复里说出来" | 论文全文 `1628`/`1,628` **各 0 次**。已改用实有的 reward 公式 `r = r_action × r_output` 与图 2d 的 54.04／41.64 |
| 02:144 | 综述"覆盖 110+ 论文、23 个系统" | 全文无此数，已删 |
| 02:144 | 交互契约 ≈ ℐ_obs | ℐ_obs 是模型侧观察接口，与本卷交互契约（失败收敛＋人机面）主体相反。已改指 𝒱+ℒ |
| 02:211 | τ-bench 标【预印】 | 已升【评审】（ICLR 2025 场次页核实） |

## 四、产品机制（均经官方文档核）

**12:48 Claude Code subagent 权限——同时是一处跨章矛盾。**
官方 `sub-agents.md`：`pre-approv` **0 命中**（"spawn 前逐一预批"查无此流程）；L754 明写 auto-deny 是 **v2.1.186 之前**已废弃的行为，现行为权限提示上浮主会话；实际机制是继承主会话工具池 + frontmatter `tools`/`disallowedTools` 静态收窄；L465 `If the parent uses bypassPermissions or acceptEdits, this takes precedence and can't be overridden`。

**最后这条正是第十一章 11:30 引为反例的那条**——同一产品同一机制，第十一章当反例、第十二章当正面样板。已按官方口径重写第十二章，结论句从"两家都把收窄钉在了委派那一刻"收窄为"钉住了能调哪些工具、没钉住调用时要不要问"，并显式认下父模式压过子、回指第十一章。第十一章事实描述准确，一字未动。

**12:56 Devin managed Devins**：官方博客 `git` 零命中，"代价是不共享 `.git`"系推断，已删，只保留 VM 隔离。顺带理顺了指代（原句让后文的"它"跨过 VM 回指 worktree）与重复代价（与本节末句撞车）。

**12:54／12:141 Cursor 3**：两轮 curl 均未核到（docs 站 JS 渲染、`.md` 端点 File not found、`llms.txt` 404）。第三轮改走 `cursor.com/changelog/3-0` 成功：官方原文 "run many agents in parallel across repos and environments: locally, in worktrees, in the cloud, and on remote SSH"、`/worktree` 创建独立 worktree、`/best-of-n` 各跑各的 worktree；3.9 条目另写 cloud agent 跑隔离 VM。**本地 worktree 与 cloud VM 两档并存**，已按实况写准。

其余：07:32 Temporal × OpenAI Agents SDK 隐式 Activity 包装、08:91 MCP Tasks（是**移出**核心协议升为官方扩展，非"新加入规范"）、10:12 prompt 缓存前缀因果说反、10:25 MCP 代码执行转述、12:62 LangGraph superstep 事务性、13:56 Anthropic 隐私承诺被弱化（"不监控对话内容"→"单独授权才采集"），均已按官方原文改。

## 五、卷内一致性（改稿在文件边界制造的新分叉，主控收口）

分文件并行改稿的副作用：A 文件改了、B 文件没跟。七处，均已收口——

- `Lingering Authority` 归属：11:30 改为"授权用完不回收"（论文全文不涉 subagent／继承）后，90:154／90:459 仍挂旧归属，已摘除
- 幂等键：工作制品 B 升三维（run_id + step 序号 + step 内 effect 序号）后，08:68 仍写二维且自称"工作制品 B 登记的裁决是…"，08:70 作用域写成"同一 step 的重试"，均已同步
- 对账边：90:475 改为"本卷新立、不承入门卷"后，13:62／13:88 仍写"继承入门卷的边定义"，已改为互补
- Model Certificate：13:90 交付物条仍是 `model version`，已同步为"生成 provenance"
- dual-write：08:24 改结构义（两写非原子，与顺序无关）后，90:94 不变量 1 仍是顺序义，已同步

## 六、四项裁决的落地

1. **第一章对第十五章的承诺** — 改第一章措辞，第十五章不动。复核确认第十五章自己已两次说清"六契约横轴承自第一章、五审查维度是本章新起的评审镜"，错在第一章单方面写错。四处已对齐，网格那处前向引用整句删除。
2. **标准故障包八→九格** — 新增 `atomicity fixture`（第六章，三处相邻写入之间 kill -9），第六章章末欠条销账；全章计数同步 11 处，SPEC 五处、README 一处跟进。实跑口径守住：未替新 fixture 报账，"三个 fixture 搭实跑接线"原句未动。
3. **第十章时变成分豁免** — 带 TTL 的 memory 与 JIT retrieval 拉进的外部正文不参与 replay 一致性，"同一份"承诺范围限于版本坐标覆盖得到的部分；工作制品 Q 同步。
4. **H 表 waiting 回路** — 不加 `interrupted→waiting`，不动 17 行计数。理由：按主线五"恢复了状态不等于恢复了旧授权"，waiting 的 run 经 interrupted 回 queued、重跑到审批点再挂起，正是本卷要的语义；缺的只是把这层意思说出来，已在 H 节表级规则与第五章正文各补一处。

## 七、规格同步（13 处）

正文改完而规格没跟会导致下轮写作把错误写回来（README 自定的规矩是"正文与 spec 冲突时先改 spec"，本轮实际是反向），故一并收：

- `SPEC.md`：441 Claude Code 预批口径｜414 Lingering Authority 归属｜157「六份不多不少」→「至少这六份」｜384 prompt cache 错因｜432/438/444 MAST 三处旧占比｜192 PCS 三件套归属与 `$1628` 案例｜472 Model Certificate 字段与对账边归属｜483/495/498/502 九格 fixture｜286 第六章交付物补 fixture
- `volume2-outline.md`：175 MAST 数字与 23.8 复合分｜196 36.4% 归属｜467 Model Certificate 字段｜471 对账边归属｜511/537 `Boundary Map` → `Runtime Trust Boundary`（工作制品 D 上一轮已全库改名，大纲漏网）
- `volume2/README.md`：26 九格
- `SPEC.md` 版本史追记 v2.22

## 八、方法论教训（写作侧长期持有）

1. **只核摘要与 HTML 正文不够——数字常在图表里，图表是图片。** MAST 的 v2/v3 版本混用第一轮 22 路审计全体漏掉，是改稿阶段的 agent 用 pypdf/pdfplumber 抽 PDF 第 2 页才发现的。凡"三大类占比""图 N 的数字"，必须抽 PDF 核。
2. **arXiv 论文会更新，引用必须钉版本号。** 同一 ID 的 v2 与 v3 数字可以完全不同（41.77/36.94/21.30 → 44.2/32.3/23.5，200+ → 1642）。本卷现已在引用条标 `v3（2025-10-26）`。
3. **分文件并行改稿必须配主控收口环节。** agent 守"不碰他人文件"的纪律是对的，但结果是七处跨文件分叉——比原错误更隐蔽。收口不能交给任何一个分组 agent，只能由有全局视角的一方做。
4. **对抗验证值回票价。** 11 路验证驳回 7 条假阳性，其中一条（"Devin 不用 worktree"）反而修正了另一路 agent 的错误建议。发现与验证必须分人。
5. **核不实就不写。** Cursor 前两轮核不到时，处置是"宁可少说不可说错"；第三轮换 URL 路径才核到。核不到不等于事实不存在，但也不构成写进书里的依据。

## 九、待裁决 / 待办

- **系统提示词的两个跨卷缺口**（用户已知，待定改法）：①第二卷用"系统约束"指代 system prompt，与第一卷"prompt asset／永不裁剪的 P0 片段"无术语桥接；②prompt asset 版本漂移与 replay 的语义无人管——`90-artifacts.md:51` invocation 行记了 `prompt_asset_ref`，但 asset 发新版后恢复该用哪版没说，且第十章刚补的时变成分豁免只点了 memory 与 retrieval 两类，未含 prompt asset。
- 01:68「不多」那半边论证缺席（三维网格只证"至少这六份"），与 2.4／2.5 的双向论证形态不齐；SPEC 已同步收窄，若要补需作者自己落笔论据。
- 02:201/204 审稿日志仍记着 `$1628` 的旧处置，正文已无此数，成悬空记录。
- 90-artifacts 正文节序 A–L → Q R S → M N O P → T–Z 未重排（已加导航说明）；出版排版阶段可纯剪贴恢复 A→Z。
- 14-build.md:133/136 与 SPEC.md:556 的"八 fixture"是历史留档，不改写；已在同格／版本史追记本轮销账。
- 上一批既定机械项（低频缩写首现、半角分号、`§` 符号文字化、配图占位清除、99-appendix 术语表节号）仍待终稿一次性扫。
