# 第二卷 vs 第三卷:分卷逻辑与第二卷架构图

> 用途:澄清"架构 / 工程 / 运维"到底怎么分卷,并给出第二卷的八组件架构图。
> 来源:第二卷 README、`volume3-outline.md`(0.1–0.3)、第二卷 §四《参考架构蓝图》。忠实原文,不加发挥。

---

## 一、一个需要纠正的说法

此前口头上把"第二卷=架构、第三卷=工程"是**不准的简化**。书**自己**的卷名是:第二卷《运行时**架构**》、第三卷《**生产**工程》。把第三卷的"**生产**工程"缩成"工程化",丢了"生产/运维"那个限定词——于是听起来像"第三卷才做工程",这是错的。

**真相:** 第二卷本身就大量是工程化(状态机、durable execution、副作用账本,第十四章"从零构造一台 runtime")。第三卷的"工程"特指**生产工程 ≈ 运维**。

---

## 二、正确的划分逻辑(书自己的原话)

第三卷大纲 0.1 写得很清楚:

> "第二卷关注**一次执行内部**发生什么;第三卷关注**一个长期运行的产品**怎样被建设、发布、监控、恢复和治理。"
>
> "第二卷保证'**执行语义不含糊**',第三卷保证'**生产承诺不靠运气**'。"

所以真正的轴**不是"架构 vs 工程"**(那是假对立,第二卷两样都有),而是**"造对" vs "运营"**:

| 卷 | 书名 | 关注 | 一句话 | 对应经典三层 |
|---|---|---|---|---|
| 一 | 入门卷 | 有哪些零件 | 一台 runtime 由哪八件部件组成、各干什么 | — |
| **二** | 运行时架构 | **一次执行内部对不对** | 把零件拼成一台**语义正确、崩了能恢复、能自证**的机器 | **架构 + 实现**(都为"正确") |
| **三** | 生产工程 | **长期产品在生产里稳不稳、久不久、划不划算** | 把这台对的机器**安全、经济地长期运营** | **运维** |

**关键分界**(第三卷大纲 0.2 明写):第三卷**不重新定义 runtime**,直接拿第二卷的 State Registry、Effect Ledger、Event Schema、cancellation/checkpoint/resume、principal/delegation、meter/limit/version 等制品,去接**数据库、网络、安全、CI/CD、发布、SRE**。第二卷讲"造对",第三卷讲"接进生产体系运营"。

第三卷的五类生产契约(服务 / 防护 / 变更 / 运营 / 经济)也印证这一点——SLI/SLO、安全边界、版本迁移、事故响应、成本止损,全是**运维**关切,不是"一次执行对不对"的关切。

**结论:** 第二卷 = **架构 + 工程实现**,主题是"**正确**";第三卷 = **运维(生产工程)**,主题是"**在真实世界里稳、久、省**"。

> 顺带:书名"运行时**架构**"其实略窄——它涵盖到"从零构造",严格说是"正确的运行时**构造**"。命名可斟酌,不影响内容划分。

---

## 三、第二卷的架构图(照 §四 蓝图,忠实原文)

第二卷有明确的**八组件**架构(§一 1.4 立、§四 摊开),分三组,每组各守一份或几份契约:

```
                         用户 / 外部触发
                    (UI · API · IM · scheduler)
                               │ 意图
                               ▼
 ┌──────────────────────────────────────────────────────┐
 │  入口层 Entry Layer          守 交互契约(呈现半边)      │  薄投影 · 只读 · 不回写
 └──────────────────────────────────────────────────────┘
                               │ 转发意图
                               ▼
 ┌──────────────────────────────────────────────────────┐
 │  run manager 单执行内核      守 转移契约 · 权限契约      │  准入·同会话互斥·policy·limit·孤儿收尾
 └──────────────────────────────────────────────────────┘
                               │ 派工
                               ▼
 ┌──────────────────────────────────────────────────────┐
 │  agent loop 一轮任务编排者   ★ 全部契约的履约现场        │  组装context→调模型→执行工具→循环
 │                             (内存即易失,自己一处都不落盘)│
 └───────────────┬──────────────────────────┬───────────┘
         调模型   │                          │  执行副作用
                 ▼                          ▼
 ┌───────────────────────┐      ┌───────────────────────┐
 │ model adapter          │      │ tool executor          │
 │ 调用记账员·留档不重调   │      │ 副作用的唯一大门        │
 │ 守 转移/证据契约(调用)  │      │ intent→执行→result     │
 └──────────┬────────────┘      │ 守 副作用契约           │
            │ 调用               └──────────┬────────────┘
            ▼                               │ 动作
         大模型                             ▼
                                     外部世界(文件·邮件·API)

════════════ 真相三件 · 持久层(上面谁都往这里落盘) ════════════
 ┌──────────────┐   ┌──────────────┐   ┌────────────────────┐
 │ 状态层        │   │ 事件流        │   │ 证据存储            │
 │ 唯一写者      │   │ 只追加        │   │ trajectory/policy/  │
 │ "现在是什么"  │   │ "怎么来的"    │   │ verifier/artifact   │
 │ 守 真相契约   │   │ 守 转移契约   │   │ "凭什么信"·守证据契约│
 │ (SQLite→PG)  │   │ (append-only)│   │ (归档·供对账回放)   │
 └──────────────┘   └──────────────┘   └────────────────────┘
```

**八组件三组:**
- **真相三件(持久层):** 状态层(唯一写者,"现在是什么")、事件流(只追加,"怎么来的")、证据存储(归档,"凭什么信")。
- **执行两件:** run manager(单执行内核,那道闸)、agent loop(一轮任务编排者,内存即易失、自己不落盘)。
- **外界三件:** 入口层(朝用户,薄投影不回写)、model adapter(朝模型,留档不重调)、tool executor(朝外部世界,副作用唯一大门)。

**三条要点(这张图就是"第二卷 = 正确性架构"的样子):**

1. **数据流**:入口层 → run manager(闸)→ agent loop →(model adapter / tool executor)→ 外界;所有落盘都进真相三件,事件流全程随行。agent loop **自己一处落盘点都没有**——产出全经别的卡落地,这正是"内存即易失"的结构表达。
2. **六契约挂在组件上**:真相→状态层、转移→状态机+事件流、副作用→tool executor、交互→入口层、权限→run manager、证据→证据存储。**架构不是画了几个框,是每条契约都有唯一的负责组件。**
3. **进程边界 = 迁移断口**(§4.7):单机基线八件同进程(SQLite 文件 + 子进程沙箱 + 进程内事件分发);负载长大时逻辑图不动,只换放法(SQLite→Postgres、进程内锁→分布式 lease、子进程→worker queue)——**换放法那一步,就是交给第三卷运维的接口。**

---

*备注:上面是 ASCII 版,研究够用。若要排版精致、可分享、可当书里 1.4 / 4.1 配图占位成稿的版本,可另做 Artifact / PNG。*
