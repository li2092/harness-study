# 术语对照表

《Harness Study》三卷（入门卷、架构与工程卷、实战笔记卷）共用的术语约定。

这份表有两个用途：

1. **给读者**：书中借用了控制论、分布式系统、数据库、安全、统计、机器学习等领域的术语，有些用法比原义宽或窄。表里逐条写明"本书用法"与"学科标准用法"的差别，读者可以对照已有知识，也可以按英文原名检索。
2. **给修订**：三卷统一译名与写法的依据。正文每个术语**首次出现**时按"首次出现写法"一列书写，之后可用简称。

标记：
- **〔借用〕**：本书借用了该术语，含义与标准用法有差别，首次出现需加一句说明。
- **〔改用〕**：原稿用法与标准用法冲突，修订时改为推荐写法。
- **〔本书〕**：本书自造的概念，首次出现需给定义。

---

## 一、全书统一约定

| 事项 | 约定 |
|---|---|
| 卷与册 | 统一称"卷"：入门卷、架构与工程卷（简称第二卷）、实战笔记卷。不用"本册""前两册"。 |
| harness | 保留英文，不译。首次出现写"harness（包在模型外面、负责上下文、工具、执行、权限与留痕的那一层程序）"。不译作"智能体框架"，避免与 agent framework（LangChain、LangGraph 这类开发框架）混淆。 |
| agent | 正文可保留 agent；需要中文时用"智能体"。 |
| turn / 轮 | 一次模型调用，加上它触发的工具执行。与 OpenAI Agents SDK、Claude Agent SDK 中 `max_turns` 的计数口径一致。 |
| 回合 | 从一条用户消息开始、到模型给出最终回复为止。一个回合包含多轮。原稿中"轮"指这个意思时改为"回合"。 |
| run / 运行 | 一次任务从开始到终态（完成、失败、取消）的全过程。 |
| session / 会话 | 同一用户与同一 agent 的一段连续交互，可包含多次 run。 |
| 历史 vs 上下文 | **历史**：持久化保存的完整消息与事件记录，是真相源。**上下文**：每一轮由历史算出来、实际发给模型的那一份。原稿中的"投影""视图"在指这个意思时统一写"上下文（由历史派生的视图）"；"改写历史"在实指改写上下文时改为"改写上下文"或"改写已发送的前缀"。 |
| 阳性方向 | 评测语境约定"判为通过"为阳性：把本该通过的判成失败叫**假阴性**，把本该失败的判成通过叫**假阳性**。 |
| 经验值 | 书中阈值与倍数，凡不是出自文献的，统一标"（经验值，按场景调整）"或写明出处与测量条件。 |
| 业界共识 | 仅在有多方一手来源支持时使用。依据是单篇预印本或单家厂商博客时，写"某某提出""某某的做法"。 |
| 数字与出处 | 引用他人数据时写明：出处、评测名、模型、基线。 |
| 标点 | 正文用中文逗号、句号、冒号。间隔号"·"只用于标题、图注和外国人名。破折号每段最多一处。 |

---

## 二、作者惯用词替换

以下词在原稿中高频出现，含义依赖作者自己的语境。修订时按表替换。**只替换作者惯用义，正常词语不动**（如"独立""成立""建立""文件""事件""条件""组件""表格""格式""严格""文档""档案""落地窗"）。

| 原词 | 典型原句 | 改为 | 说明 |
|---|---|---|---|
| 立（动词） | "第二卷 §9.6 立了一条判据""§6.7 立的配对不变量""先立口径" | 提出、给出、定义、规定、确立 | 按上下文选词；"立字据"类比喻删去 |
| 纪律 | "区分纪律""工程纪律""四条纪律" | 原则、规则、约束、做法、要求 | "四条压缩契约纪律"改为"四条压缩规则" |
| 件（量词） | "八件 runtime 机制""一件工具""这件 framing""这件确定性" | 个、种、项、类；或直接去掉量词 | "这件事""一件事"是正常汉语，保留 |
| 件（名词，指机制） | "每件机制""主归件""跨件" | 机制、组件 | 入门卷"8 件机制"改为"8 个机制" |
| 格 | "这一格""半格""补格""逐格判定""一格不动""三类都有格接得住" | 情况、位置、单元格、项、条目 | 指表格单元格时写"单元格"或"这一项" |
| 档 | "那一档""同档""一档失守""三档" | 类、级、种、层次、档位（仅指可调节的档位时） | "推理强度档位"这类真正的档位保留 |
| 落位 | "四问落位""坐标系落位" | 对应位置、归入、对应到 | |
| 欠条 | "开了一张欠条""欠条清单" | 遗留问题、待办、未完成项 | |
| 兑现、兑付 | "由第八章兑现""欠条兑付" | 实现、完成、落实 | |
| 口径 | "判定口径""厂商口径""事前口径" | 标准、判据、说法、定义、统计方法 | |
| 落地、落点、落法 | "落地形态""这一问的落点" | 实现、应用、对应位置 | 真正指"上线部署"时可保留"落地" |
| 定性 | "给这件事定了性" | 定义、判定、归类 | 与"定量"对照时保留 |
| 判据 | 节末"判据：**……**" | 仅当后文是可用来判断的条件时保留；是原则或结论时改为"要点""原则""结论"，或删去标签直接写 | |
| 点亮 | "第 2 步已经点亮的契约" | 已实现、已就绪 | |
| 照实记账、不粉饰成半格 | 章末套话 | 删去，改为一张实证状态表 | |
| 常见误区 | 入门卷 33 处 | 系统行为用"失效模式"，工程做法用"反模式（anti-pattern）"，人的错误认识才用"误区" | |

---

## 三、借用术语：本书用法与学科标准用法

### 3.1 控制论与控制理论

| 术语 | 学科标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 控制论（cybernetics） | Wiener 1948 提出，研究动物与机器中的控制与通信，核心是反馈 | 作为理解 harness 的类比框架 | 注明"本书借用控制论的若干概念作类比" |
| 可观测性（observability，控制理论） | Kalman 约 1960 年提出：能否由系统输出推断其内部状态 | 〔借用〕运行时能不能"看见"agent 在做什么 | "可观测性（此处取工程义：能看见系统在做什么；控制理论中指能否由输出推断内部状态）" |
| 可控性（controllability） | Kalman：能否通过输入把系统状态驱动到任意目标 | 〔借用〕出错时能不能干预 | 同上，说明是借用 |
| 稳定性（stability） | 扰动下系统不发散、不振荡；线性系统常用 BIBO 稳定（有界输入产生有界输出） | 〔借用〕循环能停、成本不失控 | 不把"跨 run 不可复现""reward hacking"归入稳定性 |
| 反馈 / 前馈（feedback / feedforward） | 反馈：根据输出偏差修正；前馈：在扰动作用到输出之前先行补偿 | 〔借用〕Böckeler 把 system prompt、规则等"事前约束"称为前馈 | 注明"借用 Böckeler 的类比" |
| 迭代学习控制（iterative learning control, ILC） | 在重复执行同一任务中逐轮修正 | 对应本书 Harness Lab 的"多轮迭代调参" | 替代原稿的"收敛性原则"（控制论中无此原则） |
| 扰动理论（钱学森《工程控制论》） | 在标称解附近做小扰动线性化 | 原稿把消融对应到扰动理论 | 〔改用〕消融对应的是**系统辨识（system identification）** 或受控实验，扰动理论只作名字相近的旁注 |
| 非交互控制 / 解耦控制（non-interacting control） | 设计控制器使每个输入只影响对应输出 | 原稿对应"Safety 与 runtime 正交" | 〔改用〕只作"职责分离"的类比，不称"工程硬约束" |
| 必要多样性定律（law of requisite variety） | Ashby 1956：调节器的多样性不能小于被调节对象的扰动多样性 | 原稿未用 | 建议补入：可解释"单层 verifier 挡不住多样的 reward hacking" |
| 开放的复杂巨系统（OCGS） | 钱学森、于景元、戴汝为 1990 年提出 | 引用 | 年份写 1990，不写"70 年前" |

### 3.2 分布式系统、数据库与持久化

| 术语 | 学科标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 事件溯源（event sourcing） | 以事件日志为权威记录，当前状态由事件推导；可存快照与读模型；外部副作用不靠重放重建（Fowler 专门讨论过 External Updates） | 事件表是权威账本，状态行是可重算的物化视图 | 不把"纯粹形态"写成稻草人 |
| 投影（projection） | 事件溯源中由事件流确定性推导出的读模型 | 〔借用〕给模型的上下文、给人的界面，都是由持久状态派生的只读视图 | "投影：从持久状态派生出的只读视图，可随时重建，不回写真相源"。不用来指"视角"（改"切面"）或"进程内定时器"（改"运行时缓存"） |
| 物化视图（materialized view） | 预先计算并存储的查询结果 | 状态表 | — |
| 事务（transaction） | ACID：原子性、一致性、隔离性、持久性 | 〔借用〕只取原子性："要么完整生效，要么完整回退" | "像数据库事务的原子性一样" |
| 检查点（checkpoint） | ① 一致恢复点；② SQLite/数据库中把 WAL 内容写回主库的操作 | 两义都用到 | 同一节出现两义时分别注明 |
| 快照（snapshot） | 某一时刻的一致视图 | 原稿用作"未经一致性确认的副本" | 〔改用〕改称"副本"，快照保留标准义 |
| 预写日志（WAL） | 先写日志再改数据，保证原子性与持久性；会被 checkpoint 截断 | 原稿用作"永不改写的历史"的例子 | 〔改用〕"只追加、不可改"换用会计账簿或事件日志作例子；WAL 可作"先记账再动手"的例子 |
| 只追加（append-only） | 只能追加，不可修改或删除 | 全书原则 | 不混用 append-mostly；如有例外写明例外 |
| 持久化执行（durable execution） | Temporal、Restate、DBOS 等：工作流状态与步骤结果持久化，崩溃后从历史重放恢复 | 同 | "持久化执行（durable execution）" |
| 重放（replay） | 持久化执行中按已记录的事件历史重新推进工作流代码 | 同；另有"按录下的事件回放一次 run"之义 | 评测中的"回放"写明"回放已录制的输入输出" |
| 幂等（idempotent） | 同一操作执行多次与执行一次效果相同 | 同 | 不把"去重"或"审批超时策略"称为幂等 |
| 去重（deduplication） | 按唯一标识跳过重复消息 | 按事件 ID"已应用则跳过" | "按事件 ID 去重，使处理结果满足幂等" |
| 幂等键（idempotency key） | 客户端为请求附带唯一键，服务端据此识别重复请求（如 Stripe） | 同 | — |
| 投递语义 | at-most-once（至多一次）、at-least-once（至少一次）、exactly-once（恰好一次）；投递层做不到恰好一次，处理层可借幂等做到效果上恰好一次 | 同 | 首次出现给出三个英文名 |
| 补偿（compensation）/ Saga | 长事务拆成多步，失败时执行反向补偿操作 | 同 | — |
| 租约（lease） | 带过期时间、需续约的授权 | 〔借用〕原稿的"Plan Lease""副作用租约"没有过期时间，按版本或状态失效 | "（本书借用'租约'一词，这里没有过期时间，按版本前提失效，作用接近乐观并发校验 / 围栏令牌）"；或改称"执行许可""计划前提校验" |
| 围栏令牌（fencing token） | 单调递增的编号，存储端拒绝旧编号持有者的写入（Kleppmann 2016 有经典阐述；Chubby 称 sequencer） | 原稿"世代号" | "世代号（作用即 fencing token）" |
| 乐观并发（optimistic concurrency） | 先读版本，提交时比对，不符则拒（CAS、ETag/If-Match） | 同 | — |
| 提交点（commit point） | 数据库中事务变为持久、不可撤回的时刻 | 〔借用〕副作用真正发往外部之前的最后一刻，在此复核授权 | "提交点（本书指副作用发出前的最后一刻；在此复核授权，就是在使用时检查，消除 TOCTOU 窗口）" |
| 竞态（race condition） | 结果取决于事件时序 | 原稿写"丢失是概率性的" | 〔改用〕"丢失取决于时序（竞态）" |
| 单写者（single writer） | 同一数据只允许一个写入方 | 同 | 自增序号等于提交顺序只在单写者下成立 |
| 关联标识（correlation ID） | 通常是贯穿一次请求的单个 ID | 〔借用〕原稿的 correlation 指八级层级 ID 全集 | "本书用 correlation 统称一组层级 ID，业界常见的 correlation ID 只是其中一个；传播方式类似 W3C Trace Context" |
| reducer / fold | 事件溯源：把事件序列折叠成状态的函数；LangGraph：合并并发写入的函数 | 两章各用一义 | 分别写"投影函数（fold）"与"LangGraph 的合并 reducer" |
| 熔断器（circuit breaker） | 三态：关闭、打开、半开；打开后冷却一段时间再半开试探；保护下游 | 〔借用〕原稿"断路"实指"达到阈值即强制换路或终止" | 业界中文通用"熔断"。只借"打开"这一步时写"（借用熔断器的说法，只有打开，没有冷却后的半开试探）"，或直接写"重试上限""强制换路" |
| 看门狗（watchdog） | 超时后复位或重启被监控对象 | 〔借用〕原稿只解除阻塞、不重启 | "看门狗（这里只负责解除阻塞，不负责重启）" |
| 协作式取消（cooperative cancellation） | 发出取消信号，由被取消方在检查点自行退出 | 同 | 替代"不是强制终止"的说法 |
| 编排 / 协同（orchestration / choreography） | 编排：中心协调者调度；协同：各方按事件自行协作 | 多 agent 章节 | — |

### 3.3 安全

| 术语 | 标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 最小权限（least privilege） | 只授予完成任务所需的最小权限 | 同 | — |
| 纵深防御（defense in depth） | 多层独立防护 | 同；前提是各层独立 | — |
| 失败放行 / 失败拦截（fail-open / fail-closed） | 组件出故障时默认放行 / 默认拒绝 | 原稿"观测类失败放行、安全类失败阻断" | "失败放行（fail-open）/失败拦截（fail-closed）" |
| 快速失败（fail-fast） | 尽早发现错误并立即报错 | 原稿"schema 规范化 fail-closed" | 〔改用〕"规范化失败的工具不注册（启动期 fail-fast）" |
| 引用监视器（reference monitor）/ 完全仲裁（complete mediation） | 所有访问都必须经过、不可绕过的检查点 | 原稿称"Safety 控制面" | "本书的 Safety 控制面，在安全领域的对应概念是 reference monitor" |
| 控制面 / 数据面（control plane / data plane） | 源自网络设备：控制面做决策与配置，数据面逐包转发；后被 Kubernetes、SDN 沿用 | 〔借用〕 | 注明来源是网络领域，不是 OS |
| 沙箱（sandbox） | 操作系统或容器级隔离（seccomp、容器、虚拟机、Seatbelt、bubblewrap） | 原稿也把工具层路径校验称为沙箱 | 〔改用〕工具代码内的校验称"工具层校验"；OS 级隔离写"OS 级沙箱"，不写"物理隔离" |
| TOCTOU | time-of-check to time-of-use：检查时与使用时状态不一致的竞态 | 原稿描述了现象但未点名 | "TOCTOU（检查时刻与使用时刻不一致）" |
| 提示词注入（prompt injection）/ 间接提示词注入 | 输入中夹带指令，使模型偏离原任务；间接注入指来自网页、文件、工具结果的内容 | 同 | 包装、标注来源、改用 user 角色都是**降低概率的软措施**，不称"隔离" |
| 工具投毒（tool poisoning）/ rug pull / 工具遮蔽（tool shadowing） | MCP 生态中：工具描述藏恶意指令；已批准工具的描述被悄悄改写；外部工具抢占常用工具名 | 原稿描述了现象 | 首次出现点名 |
| 混淆代理人（confused deputy） | 高权限程序被低权限方诱导，用自己的权限替对方办事 | 原稿等同于"权限透传" | 写清"child 处理不可信输入时，用父的权限办别人诱导它办的事" |
| 命令注入（command injection） | 参数中的 shell 元字符（`;` `|` `&&` `$()` 反引号）被 shell 解释 | 原稿只提分号 | 列全元字符 |
| 路径穿越（path traversal）/ Zip Slip | 通过 `../` 或链接把文件写到目标目录外；归档解包场景称 Zip Slip | 原稿"写越界" | 〔改用〕 |
| 行级安全（row-level security, RLS） | 数据库按行强制访问策略 | 原稿"过滤条件强制包含租户字段" | 点名 |
| 影子模式（shadow mode） | 新规则只记录、不生效，用真实流量评估误伤率 | 同 | 误伤率需要对"本会拦截"的样本抽样人工复核 |
| 不可抵赖（non-repudiation） | 需数字签名、哈希链等密码学手段 | 原稿指"应用层不改写历史" | 〔改用〕"历史不被应用层覆写"；防篡改需哈希链或 WORM 存储 |
| 共因失效（common-cause failure, CCF） | 多个部件因同一原因同时失效 | 原稿英文写成 common-mode failure | 〔改用〕英文改为 common-cause failure；共模失效（common-mode failure）另指以同一模式失效 |
| 能力令牌（capability）/ 基于关系的授权（ReBAC） | capability：持有即有权（如 biscuit）；Zanzibar 属 ReBAC；SPIFFE 是工作负载身份标准 | 原稿把三者归为同一类 | 分开列出 |
| X-Forwarded-For | 代理追加客户端地址的请求头，逗号分隔的链 | 原稿"转发头" | 点名；解析时从右往左跳过可信代理 |

### 3.4 统计与实验设计

| 术语 | 标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 消融（ablation） | 移除或关闭某个组件，测量对评测指标的影响 | 同 | 不用于只凭工程成本判断的情形（写"工程记录显示"） |
| 留一消融（leave-one-out ablation） | 其余组件都开，只关一个 | 原稿"留一法" | 〔改用〕"留一法"在机器学习中通常指留一交叉验证（LOOCV） |
| 逐个加入（add-one） | 从零开始逐个加入组件 | 同 | 与留一消融结果不同，不能混用 |
| 交互效应（interaction effect） | I(i,j) = f(i,j) − f(i) − f(j) + f(∅)；>0 协同，<0 负交互 | 原稿"协同""拮抗""冗余""互相替代"混用 | 统一为"正交互（协同）/负交互"；负交互的来源分"冗余（互相替代）"和"干扰（互相冲突）" |
| 基线（baseline） | 对照条件 | 原稿也用作"统计上的最低要求"或"常用检验方法" | 〔改用〕后两义改为"基本要求""常用检验" |
| 统计功效（statistical power） | 效应真实存在时检验能检出的概率 | 同 | — |
| 最小可检测效应（MDE） | 在给定样本量、显著性水平与功效下能可靠检出的最小差异 | 原稿"分辨力下限" | 〔改用〕并给出样本量 |
| 配对检验 | 同一批任务开关前后的结果配对比较：二元结果用 McNemar 检验，或配对 bootstrap | 同 | — |
| 置信区间（confidence interval, CI） | — | 同 | 用"CI 不跨 0"代替固定 N 门槛 |
| 析因设计（factorial design） | 同时改动多个因素并分离主效应与交互效应 | 原稿"同时动两个，数学上无从归因" | 〔改用〕"只跑全开和全关两组时无法归因；要么一次只动一个，要么做析因设计" |
| 混杂（confounding） | 两个因素的效应无法分开 | 原稿"单变量是数学要求" | 〔改用〕"每轮只改一处，是为了避免混杂" |
| 冒烟测试（smoke test） | 改动后立即跑的少量关键用例 | 原稿"三到五个典型用例" | 点名 |
| 空真式通过（vacuous pass） | 前提未出现，检查自动为真（来自逻辑学的 vacuous truth） | 原稿"真空通过" | 〔改用〕 |
| 独立同分布（i.i.d.） | — | cache 共谋讨论 | — |
| 冗余 / 独立 | 统计中"独立"有专门含义 | 原稿"两个命题互相独立"实指"不同" | 〔改用〕"两个命题不同" |

### 3.5 机器学习与大模型

| 术语 | 标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 前向计算（forward pass） | 自回归生成中**每产生一个 token 做一次**前向计算 | 原稿"整段输出一次前向完成" | 〔改用〕 |
| 推理（reasoning）/ 推理调用（inference） | 中文都叫"推理"：reasoning 指思考过程，inference 指一次模型调用 | — | reasoning 写"推理（思考过程）"；inference 写"模型调用"或"推理调用" |
| 测试时计算（test-time compute） | 推理阶段投入更多计算提升效果；在部分任务上存在逆向缩放 | 同 | — |
| 上下文窗口（context window） | — | 同 | — |
| 上下文工程（context engineering） | 2025 年中流行起来的说法，关注给模型提供恰当的上下文，外延包括工具、记忆、检索等 | 同 | 不把它缩窄成"只管 context 一件" |
| 中段遗失（lost in the middle） | Liu et al. 2023：多文档问答中，关键信息位于长上下文中部时**准确率**低于头尾（U 形曲线）；新模型效应有所减弱 | 原稿写"召回率" | 〔改用〕"准确率"或"利用率"，不写"召回率"（召回率是信息检索指标） |
| 上下文腐化（context rot） | 上下文整体变长时模型表现下降 | 原稿与 lost in the middle 混写 | 两者分开写 |
| 近因偏好（recency bias） | 模型更倚重靠近末尾的内容 | 原稿写成确定规律"近的那句起作用" | 写成"实践中常见"并注明因模型而异 |
| 提示词缓存 / 前缀缓存（prompt caching / prefix caching） | 缓存 prompt 前缀的计算结果（KV），命中部分按折扣价计费；**不缓存输出，不改变采样分布** | 原稿"cache 共谋"把它说成会让多次采样不独立 | 〔改用〕见下一行 |
| 响应缓存（response caching） | 对相同请求直接返回之前生成的完整输出（网关、客户端层） | 原稿未区分 | 破坏复跑独立性的是响应缓存、temperature 0 / 固定 seed、复跑间共享的文件与记忆，不是前缀缓存 |
| 上下文压缩（compaction） | 把历史替换为更短形式 | 同 | — |
| 结构化输出 / 严格模式（structured outputs / strict） | 模型侧用约束解码保证输出符合 schema（只支持 JSON Schema 子集） | 原稿与 harness 侧参数校验混写 | 分开写"模型侧严格模式（约束解码）"与"harness 侧参数校验" |
| 约束解码（constrained decoding） | 解码时屏蔽不合法 token | — | — |
| tool_choice | 请求参数，控制模型是否必须调用工具（auto / required / 指定工具） | 原稿"请求参数层的强制" | 点名 |
| 并行工具调用（parallel tool calls） | 一次回复中发出多个工具调用；OpenAI 于 2023-11 率先支持 | — | — |
| LLM-as-a-judge | 用模型作评审 | 同 | — |
| 可验证奖励强化学习（RLVR） | **训练范式**：用可程序判定对错的规则给奖励 | 原稿与运行时 Hard Gate 混为一谈 | 〔改用〕运行时的确定性检查称"Hard Gate"；同类信号用于训练时才叫 RLVR |
| 过程奖励模型 / 结果奖励模型（PRM / ORM） | 需训练的奖励模型，主要用于训练与推理期搜索（best-of-N、beam search） | 原稿列为运行时 verifier 第三层 | 注明"线上作为 gate 较少见" |
| 奖励投机（reward hacking）/ 规格投机（specification gaming） | 模型钻奖励或规格的空子；specification gaming 是总称 | 同 | 分类互斥，不重复列"长度投机" |
| 偏好泄漏（preference leakage） | Li et al. 2025：用某 LLM 生成的合成数据训练的学生模型，会被与该数据生成模型相关的 judge 偏袒 | 原稿简化为"judge 与 agent 相关" | 〔改用〕按原论文表述 |
| 轨迹（trajectory） | 强化学习与评测中：状态-动作（-观察）序列 | 本书指持久化的执行事件流及其视图 | 注明本书用法 |
| 追踪 / 链路（trace）/ span | OpenTelemetry：由埋点直接产出的 span 树 | 本书也有"由事件派生的诊断视图"之义 | 注明差别 |
| OTel GenAI 语义约定（semantic conventions） | OpenTelemetry 针对生成式 AI 的属性命名约定 | 原稿说与 W3C Trace Context"同源" | 〔改用〕W3C Trace Context 是跨服务传递 trace 标识的请求头格式，GenAI semconv 是 OTel 内部的属性约定，二者是两层 |
| 记忆分类 | 认知科学：工作记忆（Baddeley & Hitch 1974）、情景、语义、程序性记忆；CoALA（Sumers et al. 2023）把它借用到语言智能体 | 原稿把 Memory 定义为"跨 turn 的工作记忆" | 〔改用〕工作记忆对应本书的 Context；本书 Memory 指"harness 在模型外维护的、跨多次调用可读写的状态"；四分类是类比，不是等价 |
| 系统辨识（system identification） | 通过施加输入、观察输出推断系统特性 | 消融、把脉（行为探测） | 替代"扰动理论" |
| 行为探测（behavioral probing） | 用设计好的输入探测模型行为 | 原稿"把脉" | "行为探测（本书也称'把脉'）" |
| 支架 / 脚手架（scaffold / scaffolding） | SWE-bench、METR 等文献常用，指包在模型外的程序，与 harness 近义 | 原稿说"工程师觉得别扭" | 〔改用〕写明研究界仍常用 |
| 智能体-计算机接口（ACI） | SWE-agent（Yang et al. 2024）提出：按 agent 的使用方式设计工具接口 | 同 | 首次写全称 |
| 工具检索（tool retrieval）/ 动态工具选择 | 按查询从大量工具中检索子集注入 | 原稿"Skill-RA" | 〔本书〕Skill-RA 注明"本书用语，业界常称 tool retrieval"。动态子集会改动工具段，**降低**提示词缓存命中率 |
| 子智能体（sub-agent）/ 交接（handoff） | handoff 是控制权转移；OpenAI Agents SDK 默认把完整对话历史交给接收方，可用 input_filter 裁剪 | 原稿"不共享状态、隔离最强" | 〔改用〕 |

### 3.6 软件工程与程序语言

| 术语 | 标准用法 | 本书用法 | 首次出现写法 |
|---|---|---|---|
| 效应系统（effect system） | 程序语言中在类型层静态追踪副作用 | 原稿"把工具层当作 Effect System" | 〔改用〕改称"副作用管理层"，或注明"借用名字，指运行时的登记与网关" |
| 类型状态（typestate） | 用类型编码对象状态，非法状态转移编译不过 | 原稿称可拦截模型运行时的工具调用 | 〔改用〕只能约束 harness 开发者写的代码；模型运行时发出的调用仍需运行时策略检查 |
| 可执行规格（executable specification） | 机器可运行的规格：测试、TLA+ 模型、Gherkin 场景 | 原稿指文字规格 | 〔改用〕文字规格称"规格"；写成可运行的一致性测试（conformance test）时才称可执行规格 |
| 不变量（invariant） | 始终为真的性质 | 原稿把"跨 turn 会变的 hash"称为不变量 | 〔改用〕 |
| 正交（orthogonal） | 彼此完全独立、互不影响 | 原稿"三轴正交"，但拓扑会约束交互方式 | 〔改用〕"相对独立，但有约束" |
| 本体（ontology） | 知识工程中概念、关系及语义约束的形式化体系 | 原稿"5 维度本体"指领域模型 schema | 〔借用〕注明"借用'本体'一词，这里指领域模型 schema" |
| 设计模式（GoF） | Factory Method、Abstract Factory 等 23 种 | 原稿"Factory" | 写全名 |
| 签名 / 代码签名（code signing） | 对二进制做密码学签名以证明来源 | 原稿指代码版本的指纹 | 〔改用〕"代码指纹"或"代码版本标识"；整组记录称"溯源信息（provenance）" |
| 指纹（fingerprint） | 对内容取哈希 | "语料指纹" | 同 |
| 冒烟测试、回归测试 | — | — | — |
| 快速失败（fail-fast） | 见 3.3 | — | — |
| 流式（streaming） | LLM 语境：token 逐段返回（SSE） | 原稿也用作"逐轮累积上下文" | 〔改用〕后者改为"增量累积" |
| 白名单 / 黑名单（allowlist / denylist） | — | 原稿把 disabledTools 称为白名单 | 〔改用〕 |
| 测试驱动开发（TDD） | 先写失败测试，再写实现 | 原稿标题"通用 TDD 提示词"实为评测先行 | 〔改用〕 |

### 3.7 协议与产品（事实性写法）

| 项目 | 推荐写法 |
|---|---|
| MCP 传输方式 | stdio 与 Streamable HTTP（2025-03 起取代初版的 HTTP+SSE）。WebSocket 不是 MCP 标准传输方式。 |
| OWASP LLM Top 10（2025） | LLM06 Excessive Agency（过度代理）、LLM08 Vector and Embedding Weaknesses、LLM10 Unbounded Consumption。引用时写版本年份。 |
| A2A | Google 2025-04 提出，后捐入 Linux Foundation。 |
| handoff 默认行为 | 见 3.5 |
| 产品发布时间 | 以官方公告为准，见附：待核实与已核实事实 |

---

## 四、应点名的业界标准术语

原稿描述了这些现象，但没有写出通用名，读者无法对照或检索。首次出现写"中文说法（英文原名）"。

TOCTOU · X-Forwarded-For · NO_PROXY / no_proxy · SIGHUP / setsid / nohup · React StrictMode · AbortController · lastIndex（JavaScript 带 g / y 标志的 RegExp）· tool_choice · finish_reason / stop_reason · LC_ALL=C · Zip Slip · 命令注入 · RLS · fail-open / fail-closed · 影子模式 · at-least-once / at-most-once / exactly-once · tool poisoning / rug pull / tool shadowing · ACI · reference monitor · smoke test · MDE · vacuous truth · Job Object / KILL_ON_JOB_CLOSE / CREATE_BREAKAWAY_FROM_JOB · GIT_TERMINAL_PROMPT · core.hooksPath / core.fsmonitor / core.pager / diff.external · CVE-2024-32004

---

## 五、本书自造概念

保留的概念在首次出现时给定义，并收入各卷术语表；不保留的换成平实说法。

### 5.1 保留并定义

| 概念 | 定义（建议首次出现时写） | 出处 |
|---|---|---|
| Harness Lab | 本书对"用评测、消融、调参迭代改进 harness 的外层工作台"的命名；业界相近叫法有 meta-harness、autoresearch、outer loop | 入门卷 §7 |
| 观测面（observation surface） | agent 从环境得到的反馈（工具结果、错误、状态）的设计面；与面向人的可观测性（observability）不同 | 入门卷 §5.6 |
| Safety 控制面 | 每次工具调用都必须经过、不可绕过的检查层；安全领域的对应概念是 reference monitor | 入门卷 §5.9 |
| Hard Gate / Outcome Judge | 运行时确定性检查 / 模型评审；本书的 verifier 三分法是本书归纳，不是业界标准术语 | 入门卷 §5.8 |
| cache 共谋 | N 次复跑因响应缓存、固定 seed、共享状态而不独立的问题（**不是**前缀缓存导致） | 入门卷 §7.4 |
| 六契约 / 四问 / 八组件 | 第二卷的组织框架；写明"本书的一种组织方式，未证明完备" | 第二卷 §1 |
| 证据面（Evidence Plane） | 系统为事后核查留下的全部记录，作为一层专门设计的部分 | 第二卷 §13 |
| 协调面 | 多 agent 首先带来的协调工作：接口、边界、何时算完、回传信不信 | 第二卷 §12 |
| 标准故障包 | 一组预设的故障用例（fixture），用于逐项判定 runtime 是否满足契约 | 第二卷 §14 |
| 检测器自检（sabotage validation） | 定期给检测器喂已知坏样本，确认它还能报警；思路同合成探测、mutation testing；"零事件"检测类似 dead man's switch 或 Prometheus 的 absent() | 第二卷 §13 |
| 塑形内容 | 塑造模型行为的开场内容：system prompt 中的角色、few-shot 示例、输出格式、流程模板；与"环境信息"（时间、路径、工作区状态等）分开处理 | 第二卷 §10、实战笔记卷 §8 |
| 判例 | 真实发生过的故障案例，带编号，用作评测用例的来源 | 实战笔记卷 §12 |
| 能力开关表 | 可以关闭、关闭后系统仍能完成同一类任务的机制清单；只有这类机制适合做消融 | 实战笔记卷 §13 |
| 决策事件 | 机制每次检查都发出的事件（无论是否命中），用来区分"没运行"和"运行了但无事可报" | 实战笔记卷 §12 |
| 反模式编号 AP01–AP19 | 本书附录的反模式清单编号；正文首次出现写"名称（AP10）" | 入门卷附录 F |

### 5.2 替换为平实说法

| 原稿 | 改为 |
|---|---|
| 继续站点 | 循环在哪些位置决定继续下一轮 |
| 一等设计对象 | 需要单独设计的部分 |
| 闸门 / 闸前预判 | 检查 / 权限检查；提前按计数判定会被拒 |
| 结算 / 落终态 | 登记结果 / 标记为已结束 |
| 写权 | 写入许可 |
| 终态记账 | 记录本轮以什么方式结束 |
| 分辨力下限 | 最小可检测效应（MDE） |
| 中转层 | 客户端与模型 API 之间的代理层 |
| 在场性 | 某个片段在任何裁剪档位下是否都会出现 |
| 纪律级 / 机制级 | 靠开发者记得做 / 由类型系统或统一入口强制 |
| 执行器前缀 | 把后续内容当命令执行的包装（sudo、env、xargs、bash -c） |
| 写越界 | 路径穿越 |
| 后台符号 | `&` |
| 触达率 | 真实运行中碰到该上限的比例 |
| 硬门 | 代码可判定的硬性检查（Hard Gate） |
| 真空通过 | 空真式通过（vacuous pass） |
| 静态实质面 / 沙箱事实 | 用例的静态内容 / 沙箱初始状态 |
| 验证三件套 | 编译（或构建）、测试、类型检查三项全部通过 |
| 完成分级 | "完成"的分级定义 |
| 两轮断路 | 同一问题连修两轮无效就停 |
| 反向差距 | 前两卷已有、本卷只简要提及的内容 |
| 分边 | 判定某一步在恢复时是重放还是重新执行 |
| 账本一侧 | 恢复时复用记录、不重新执行的一类 |
| 降维 | 简化到单机 |
| 地板 | 最低保证 |
| 第一天智能水平 | core 仍按上线当天的模型能力与假设设计 |
| 副 harness | 按任务动态调起的、带领域规则的子 harness（首次出现定义后可保留） |
| 强 / 弱 reward | 可被程序自动判定的奖励（verifiable reward）/ 需人或模型评判的奖励 |
| 判定线 | 判断标准 |
| 把脉 | 行为探测（behavioral probing） |
| 同步函数（field-notes §8） | 状态切换函数 |
| 制表符（field-notes §6） | 框线字符（─ │ ┼ ═ \| - +） |

---

## 附：事实核查

修订中涉及的外部事实，以一手资料核对后记录在此（出处与结论）。核查进行中，完成后补全。
