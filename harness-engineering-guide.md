# Agent Harness 的工程化：定义、内容、做法与陷阱

日期: 2026-07-09
用途: 教程原料。系统化整理 "harness 领域的工程化" 的定义、内容框架、常见做法与注意事项。
材料: 5 路专题研究笔记（传统状态持久化理论、学术故障研究 ×2、业界实践、durable execution）+ Howpot Chat 韧性摸底的一手实证（`howpot-chat/docs/resilience-audit-2026-07-09.md`）。

**来源分级约定**（本文严格执行，写教程时建议保留）：
- 【评审】同行评审或大样本实证的学术工作
- 【预印】arXiv 预印本，未经同行评审，标注局限
- 【官方】产品官方文档/官方博客
- 【推导】多条理论叠加后针对具体场景的综合推导，非某文献原话
- 【经验】本项目一手实证或模型知识，未经本次外部验证

---

## 一、定义

### 1.1 什么是 harness

Harness 指包住 LLM agent 核心循环的运行时外壳。模型本身只做一件事：给定上下文，产出下一段文本或工具调用请求。把这个无状态的推理能力变成一个能持续工作、可被打断、出错能恢复的系统，中间隔着的全部基础设施就是 harness。

这个词经历了一次含义迁移。早期 "harness" 几乎专指 evaluation harness——跑基准测试的脚手架（SWE-bench、WebArena 的容器隔离与打分器）。2025 年起，业界和学界开始用它指代 runtime harness：生产环境里承载 agent 运行的那层系统。学术界最近给出了可操作的形式化定义，Harness-Bench 将其描述为"调控模型调用并将模型输出转化为外部工作空间中操作的系统层"【预印, arXiv:2605.27922】；另一篇综述把 harness 分解为六元组【预印, arXiv:2606.20683】：

1. **观察接口**（ℐ_obs）：把环境信号转成模型可用的输入
2. **上下文管理**（𝒞）：决定什么信息进入当前调用、如何压缩与排序
3. **控制循环**（ℒ）：观察-推理-行动-反馈的调度、重试、模型路由
4. **动作接口**（ℐ_act）：模型输出映射为可执行操作（工具执行）
5. **状态与产物存储**（𝒮）：会话历史、checkpoint、日志、diff、记忆持久化
6. **验证与治理**（𝒱）：测试、验证、回滚、预算控制、审计轨迹

这六块可以直接当教程的分析骨架。本文第四章的十二个机制域是这个六元组在桌面 agent 应用上的展开。

判定"一个系统算不算 harness"另有一个更锋利的工具——**T1-T4 四要件测试**【预印, arXiv:2606.10106】：

- T1 agent loop：下一步依赖上一步观察，不是单次生成
- T2 tool interface：工具能真正改变环境，不是只读
- T3 context management：按任务相关性主动筛选进出上下文的内容，不是机械按长度截断
- T4 control mechanisms：guardrail/验证/确定性 handler **独立于模型是否"愿意"配合**

四条里最常被忽略的是 T4：把压缩、权限确认做成 prompt 层软约束（指望模型自觉遵守）的实现不满足 harness 定义——模型一旦不配合就失控。真正的 harness 级控制走在模型输出之外的硬路径上。

还有一个术语提醒：不加限定词时，当前业界的 "harness" 默认指 runtime harness；"evaluation harness"（评测脚手架）在事后运作，agent harness 的控制发生在执行中。文档里混用会让读者误判在讲评测框架。

### 1.2 什么是 harness 的工程化

一句可操作的定义：**工程化 = 让系统在"多机制交叉生效 + 故障 + 用户干预"时行为可预期**。展开为三条判定标准：

- **每个状态有归宿**：任何机制的任何中间状态，都明确定义了它在正常路径、异常路径、被打断路径上分别去哪。没有"不知道会怎样"的状态。
- **每个故障有路径**：进程崩溃、网络断开、磁盘写满、上游超时——每类故障发生时，系统知道自己该做什么（恢复、降级、标记、告知），而不是行为取决于故障恰好发生在哪一行代码。
- **每个中断可恢复**：用户切走、点停止、关窗口、导航离开，之后回来，系统能接上——至少能诚实告知丢了什么。

与功能开发的区别在提问方式。功能开发回答"这个机制自己怎么工作"；工程化回答"这个机制和已有的每个机制交叉时，每个组合状态的归宿是什么"。前者的工作量随机制数线性增长，后者随机制对数量组合增长——这正是工程化必须靠框架和清单、不能靠临场想到哪写到哪的原因。

## 二、核心命题：harness 工程质量是独立变量

教程需要先立住一个论点：agent 表现不好，很多时候不是模型不行，是 harness 不行。这个论点现在有了定量证据。

**同一批模型，不同 harness，成功率差 23.8 个百分点。** Harness-Bench 冻结任务集、沙箱初始状态、预算、超时和评估器，只变化 harness 配置，8 种模型后端下最好与最差的 harness 相差 23.8pp【预印, arXiv:2605.27922】。

**多数 agent 失败源于系统设计，不是模型推理能力。** MAST 对 7 个主流多智能体框架、1600+ 条标注轨迹的实证分类（标注一致性 Cohen's Kappa 0.88，唯一有统计效度验证的 agent 故障分类法）给出三大类故障占比：规范问题 41.8%、agent 间失调 36.9%、任务验证 21.3%——三类都是 harness/系统设计层的问题。干预实验更直接：不换模型不改用户 prompt，只给 ChatDev 加一层对照任务目标的语义验证，成功率 +15.6pp；只改进角色规范设计，+9.4pp【评审, arXiv:2503.13657, NeurIPS 2025 D&B spotlight】。

**改进不可叠加，回归难以预料。** 三个独立 harness 组件分别改进共 +11.1pp，组合后只有 +7.3pp（非加性）；模型预测"这个改动修好什么"的精度 33.7%，预测"这个改动会弄坏什么"只有 11.8%【预印, arXiv:2604.25850】。这条证据支撑两个工程纪律：不要用局部收益线性外推总收益；harness 改动必须跑回归测试，不能靠"看起来没影响别的"。

Howpot 摸底提供了同一命题的反面实证：审计确认的 5 条 P0 全部是 harness 层缺口（持久化时机、抢占语义、导航防护、退出流程、备份一致性），没有一条与模型能力有关【经验】。

## 三、理论根基:从传统软件工程借什么

Harness 是新词，但它面对的每个问题——状态、崩溃、并发、副作用——都有几十年的理论积累。教程应该先讲这些根基，再讲 agent 特有的部分，否则读者会把普适问题误认为 agent 新问题。

### 3.1 真相源：SSOT 与轻量事件日志

SSOT 的严格定义是每个数据元素只在一处被 mastered，其余位置只允许只读引用或带调和机制的副本【官方, Wikipedia: Single source of truth】。对 agent 应用的直接推论：数据库里的 run/message/tool_output 表是唯一 mastered 位置；前端 UI 状态、SSE 推流缓冲、内存中的 context window 全部是它的只读投影。一旦允许 UI 状态反向定义真相，崩溃恢复就没有基准可依。

完整 Event Sourcing（状态完全由事件重放导出、不存当前态）对 agent 应用通常是过度设计——工具副作用是外部世界的改变，无法靠重放内存事件重建。Fowler 本人明确警告：只有确需审计、回溯纠正历史或多个并行读模型时才值得引入，且这是难以事后重构的架构决策【官方, martinfowler.com/eaaDev/EventSourcing.html】。适合 harness 的是退化版：一张 append-only 的轻量事件表，记录"发生过什么"用于排障和崩溃后定位，但不充当唯一状态源。Kleppmann 的 log-as-truth 思想同理降维使用：所有写先归一到一条有序日志（单机可以就是 SQLite 里一张带自增 id 的表），缓存、UI、统计视图都作为它的消费者重建，从根源消灭 dual write【官方, kleppmann.com, 2015】。

### 3.2 崩溃一致性：WAL 这层不要自己发明

数据库理论用 Write-Ahead Logging + ARIES 三阶段恢复（Analysis/Redo/Undo）解决"崩溃后恢复到精确状态"【官方, sookocheff.com 对 ARIES 的讲解】。关键工程判断：SQLite 已在文件级别提供这套保证，应用层不需要重新实现，只需要遵守它的边界假设——**一个逻辑操作对应一个 SQLite 事务**。

SQLite WAL 模式的具体语义值得单独记住【官方, sqlite.org WAL 文档】：
- 写入追加到 `-wal` 文件而非直接改主库；读者与写者互不阻塞，各自看到一致快照——恰好匹配"流式生成边写库、前端边轮询"的场景。
- Checkpoint 分 PASSIVE/FULL/RESTART/TRUNCATE 四种；日常靠自动 PASSIVE（约 1000 页触发），应用退出或空闲窗口主动跑一次 TRUNCATE 防止 `-wal` 无限增长。
- **备份陷阱**：只拷 `.db` 不带 `-wal` 会丢失已提交但未 checkpoint 的最新事务。正确做法是用 backup API，或确保无活跃写者时三文件一起拷。Howpot 的 autoBackup 恰好踩的就是这条【经验, 摸底 P1】。

### 3.3 副作用与幂等：dual write、outbox、意图-结果两段式

"写数据库"和"执行工具产生文件/网络副作用"是一次典型 dual write：二者不共享事务边界，进程在两次写之间崩溃就会留下永久不一致【官方, AWS Transactional Outbox Pattern】。单机 agent 的可行解不是分布式事务，而是排出严格顺序的**意图-执行-结果三段落盘**【推导】：

1. 执行前原子写一条 intent 记录（run_id、step 序号、工具名、入参、幂等 key）
2. 执行副作用
3. 执行后原子写一条 result 记录（成败、输出摘要）

崩溃恢复时，"有 intent 无 result"的记录标识出副作用状态未知的操作：幂等安全的可重试，不可重试的明确标记"状态未知"交人工判定——而不是假装能自动回滚已写到磁盘的文件。幂等 key 要派生自持久化日志的单调位置（run_id + step 序号，或 rowid），不要用应用层自维护的计数器，后者在并发下会乱序【官方, morling.dev/blog/on-idempotency-keys】。

投递语义的对应关系一并记住：exactly-once delivery 在分布式条件下不可达成，工程上能做到的是 at-least-once 投递 + 幂等消费 = exactly-once processing。

### 3.4 容错哲学：crash-only 与 supervision

**Crash-only software**（Candea & Fox, HotOS IX 2003【评审】）的核心论点：如果系统只有"崩溃"一种停止方式、只有"恢复"一种启动方式，那么这条唯一的路径会被反复锤炼到可靠；反之，优雅关闭与崩溃恢复两条路并存时，后者极少被真正测试，真崩溃时必然出问题。前提条件是**重要状态外置**到 crash-safe 的存储（数据库/事务日志），进程本身可以随时丢弃。对 harness 的启示：**把崩溃恢复当第一公民设计，优雅路径只是优化**。启动时扫描 status=running 的孤儿 run 并收敛（标记 interrupted），这条逻辑天然同时覆盖崩溃、强杀、断电——比给每种退出方式单独写清理逻辑可靠得多。

**Erlang/OTP 的 let-it-crash + supervision tree**【官方, erlang.org Supervisor Behaviour】：进程隔离让单点失败不扩散，监督者按三种策略处理子进程死亡——one_for_one（只重启死掉的那个，适用相互独立的子进程）、one_for_all（全部重启，适用紧耦合必须同步的）、rest_for_one（重启死者及启动顺序在其后的，适用有启动依赖的）。同样重要的是**重启强度限制**：MaxT 秒内重启超过 MaxR 次，supervisor 终止全部子进程并自我了断，把问题上抛给上级——防止重启死循环，让"救不活"成为一个显式状态而非无限挣扎。单机降维：Tauri 壳监督 server 子进程（检测退出、退避重启、连续失败呈现诊断页——这就是 supervisor 自我了断后的"上抛给人类"），server 监督工具子进程（abort 时确保回收，不留孤儿）。重启逻辑本身必须带上限，无界自动重启比不重启更危险。

**《Release It!》稳定性模式**（Nygard【经验, 经典书籍, 本次未逐章核对】）在单机的降维使用：timeout（一切等待都有上限——LLM 调用、工具执行、确认挂起、子进程回收）、circuit breaker（同一依赖连续失败后停止重试、快速失败并告知，本文成文过程中对卡死 workflow 的处理就是实例）、bulkhead（资源隔离，单机对应"一个会话的故障不拖垮其他会话"）、fail fast（能提前判定失败的在入口就拒绝，不拖到深处才炸）。

**失败模式三分**【经验】：fail-fast（立即暴露，适合开发期与数据完整性问题）、fail-safe（降级续行，适合非关键路径如通知、统计）、fail-silent（静默，几乎永远是错的——见 5.3 与 4.10 的沉默失败）。工程化的要求是每处失败**显式选择**属于哪类，而不是让 catch 块的写法偶然决定。

**SRE 视角**【官方, sre.google】：可观测性三支柱（logs/metrics/traces）在单机 agent 的对应形态——结构化事件日志、关键计数指标（run 成败、恢复次数、压缩次数）、trace 即 transcript（一次 run 的完整可回放记录）。Error budget 思想降维为"可靠性投入的止损线"：不追求零故障，追求每类故障都有预算内的处理路径。

### 3.5 单机降维总原则

传统理论多为分布式系统写就，桌面 agent 是单机单写者，照搬会过度设计。三条降维判断【推导】：
- 共识、分区容忍、分布式事务：不需要。单机 SQLite 单写者天然串行。
- WAL、事务边界、幂等、supervision、timeout：原样适用，且成本极低。
- Event sourcing、CQRS、outbox：取其形不取其重——轻量事件表、只读投影、意图-结果两段式就是它们的单机形态。

---

## 四、内容框架:harness 工程化的十二个机制域

每个域按同一模板整理：**职责 → 必须回答的问题 → 常见做法**。"必须回答的问题"是工程化的实质——设计评审时逐条过一遍，回答不出的就是缺口。

先给一个跨厂商对照结论【经验+官方，综合 Anthropic/OpenAI/LangGraph 文档研究】：各家 harness 设计已收敛到同一组必备件——run 生命周期状态机、transcript/state 持久化、断点续跑（resume）、工具确认/挂起、上下文压缩与压缩边界记录、abort 语义——区别只在实现位置。Anthropic 把复杂度问题前置为"该不该用 agent"的决策（能用可枚举 workflow 解决的不上自主 agent）；OpenAI 把状态机做成 API 层显式契约；LangGraph 把持久化做成可插拔的 checkpointer 抽象。自研 harness 缺了清单里任何一件，都会在某个交叉场景付出代价。

### 4.1 Run 生命周期状态机

职责：一次 agent 执行从发起到终结的状态管理。

必须回答：状态集合是什么（至少 pending/running/waiting_input/completed/failed/interrupted/aborted）？每个状态由谁写入、什么事件触发迁移？进程重启后发现 status=running 的孤儿怎么收敛？同一会话来了新请求，旧 run 的归宿是什么？

常见做法：
- **显式状态机 + 启动扫描**：run 状态存库而非只在内存；进程启动时把所有 running 收敛为 interrupted，UI 对 interrupted 有明确呈现。OpenAI Assistants API 的 run 状态机（queued/in_progress/requires_action/cancelling/expired...）是公开的参照实现，expired 状态专门处理"没人管的挂起 run"；run 非终止时锁定整个 thread，用锁而非乐观并发防竞态【官方】。
- **状态机契约需要客户端超时兜底**：OpenAI 开发者社区反复出现 run 卡在 queued/in_progress、requires_action 死循环的故障——纯服务端状态迁移不够，客户端侧必须有超时与重试，否则卡死对用户不可见也无法自愈【经验, 社区故障报告归纳】。
- **抢占必须先收尾**：替换同会话旧 run 前 abort 并等待其持久化完成（Howpot 摸底 P0-2 的教训：不等待的抢占 = 静默丢一整轮）【经验】。
- **终结原因入库**：stopped/timeout/error 区分记录，下游（UI 标注、上下文压缩）都依赖它。

### 4.2 持久化时机

职责：决定什么时候落什么盘，使任意时刻崩溃后可恢复、可判定。

必须回答：run 开始落什么？过程中以什么粒度落？结束落什么？崩溃后靠什么标记物定位进度？哪些操作必须在同一事务？

常见做法【推导，综合 SQLite WAL 语义 + outbox + Temporal/LangGraph checkpoint 模式】：
- run 开始：一次原子 INSERT 落 run 记录含用户输入——这是该 run 存在的 SSOT 锚点，也是崩溃扫描入口。
- 过程中：按**事件粒度**落 append-only 记录（消息片段完成、工具 intent、工具 result 各一条，各自独立小事务），不逐 token 落盘（无恢复价值且频繁 fsync 拖垮 WAL），也不把整个 run 包成一个大事务（丢失全部增量恢复能力）。
- run 结束：一次原子 UPDATE 收口状态与最终结果。
- 恢复：status=running 定位异常中断的 run → 事件表最后一个已完成 step 做 checkpoint → 从那之后继续或标记失败。这是 Temporal event-history 重放和 LangGraph checkpointer 的单机简化形态【官方】。

Durable execution 世界还有三个可轻量借用的细节【官方, Temporal 文档】：
- **确定性约束**：Temporal 要求 workflow 代码可重放，一切非确定操作（取当前时间、随机数、UUID）必须隔离到 Activity/Side Effect。单机对应：恢复逻辑依赖的判定量（step 序号、幂等 key）必须来自持久化记录，不能来自重算。
- **外部交互全部 Activity 化**：每个 LLM 调用、工具执行、API 请求做成可独立重试、可隔离失败的离散步骤——Temporal 与 OpenAI Agents SDK 的集成（2025）正是按这个原则把 agent 变成 durable workflow 的。
- **历史有容量上限就要有"续新"机制**：Temporal 的 Event History 超 51200 条强制终止，官方解法是 Continue-As-New（携带摘要状态开新执行）。这与 agent 的上下文压缩是同构问题——历史无限增长的系统必须内建"带摘要重开"的机制，且边界要显式记录（呼应 4.8 的压缩边界）。

### 4.3 流式传输与重连

职责：server 到 UI 的增量输出通道，以及断开后的续传。

必须回答：客户端断开，server 端 run 的去向？有没有"重新订阅进行中 run"的通道？出错时流的终结信号是什么？断网时靠什么发现连接已死？

常见做法：
- **SSE 协议原生就有重连语义**——`id:` 字段 + `Last-Event-ID` 请求头 + `retry:` 字段，浏览器 EventSource 自动带 Last-Event-ID 重连；server 端维护事件缓冲即可实现续传。多数自研实现（包括用 fetch 流替代 EventSource 的）放弃了这层免费能力，需要自己补【官方, WHATWG SSE 规范】。
- **快照 + 增量**：重连时先发一个当前状态快照再续增量，避免缓冲无限增长。
- **错误也要有终结信号**：出错分支必须发终止事件（等价 [DONE]），否则客户端永远等待（Howpot 摸底 P1：出错分支缺 [DONE] → UI 永远转圈）【经验】。
- **心跳**：SSE 注释行做 keepalive，客户端读超时判死。

### 4.4 中断语义分层

职责：区分并正确处理不同层次的"停止"。

必须回答：UI 断开、用户点停止、会话删除、进程退出——各自意味着什么？abort 传播到哪一层为止？工具子进程谁回收？

常见做法：
- **明确分层**：客户端断开 ≠ run 终止（后台跑完是合理设计）；用户 Stop = run 终止但已生成内容保留；进程退出 = 等待或快速持久化后终止。每层语义写成文档。
- **abort 信号全链路贯通**：从 HTTP 层 AbortController 到 agent 循环到每个工具的 execute()，任何一环不接收 signal 就会出现"假停止"（Howpot 摸底 P1：Stop 后 shell 子进程继续跑 5-10 分钟）【经验】。
- **中断可恢复的最小状态集**：AgentBay 给出中断点要捕获的三元组——内存/上下文、工具输出历史、当前推理链；恢复时必须显式告知 agent "发生过介入及其内容"，不能假装无事续跑，否则出现状态一致性与信念更新错配【预印, arXiv:2512.04367】。

### 4.5 人机挂起（确认、提问、审批）

职责：agent 等人回应期间的状态管理。

必须回答：挂起状态持久化吗（刷新/重启后还在吗）？有超时吗，超时后 run 去哪？挂起期间占用什么资源（全局锁？）？用户在别处回应了怎么同步？

常见做法：
- **挂起即持久化**：LangGraph 的 interrupt 机制把挂起做成一种 checkpoint——工作流状态落库，进程可以死，几天后 resume 从断点继续；因为 checkpoint 不可变且带版本号，同一套机制还免费解锁 time-travel（从任意历史 checkpoint 分叉重跑）【官方, LangGraph interrupts 文档】。Temporal 同理：human-in-the-loop 等待数天靠的是挂起态在持久层而非内存【官方】。存储实现分档（内存/SQLite/Postgres）做成可插拔抽象，但内存档只许开发用——上生产等于重启全丢且无告警。
- **超时是必需品**：挂起无超时 + 全局单 run 锁 = 一次没人理的确认能永久锁死系统（Howpot 摸底 P1 实证）【经验】。OpenAI 用 expired 状态兜底，Temporal 用 timer 与 workflow 超时兜底——业界没有无限期裸等的实现。
- **外部干预的三原语**：Temporal 把"外界与运行中工作流交互"收敛为 signal（单向注入）、query（只读查询状态）、update（能返回值的同步注入）三种原语，human-in-the-loop 即"挂起等 signal/update"【官方, Temporal 文档】。自研 harness 的确认/回应接口按这三类归位，语义就不会含糊。
- 挂起必须响应 abort：Stop、删除会话都要能解除等待。

### 4.6 工具执行与副作用

职责：模型的动作接口，及其与真实世界副作用的对账。

必须回答：副作用与消息记录如何保持一致（见 3.3 意图-结果两段式）？工具入参校验在哪层？危险操作的确认门槛？执行中的输出如何流式反馈？超时与资源上限？

常见做法：
- 意图-结果两段落盘 + 幂等 key（3.3 详述）。
- **校验三层缺一不可**：格式（schema/exit code）之外，还要语义（输出是否达成任务目标——MAST 实证加这层 +15.6pp）和编排（当前状态是否符合预期转移）两层，只做 schema 校验会漏掉语法正确但逻辑错误的两类隐蔽故障【预印, arXiv:2606.01416；评审, arXiv:2503.13657】。
- 权限随任务生命周期收回，警惕"权限蔓延"（任务结束权限还在）【预印, arXiv:2606.22504】。

### 4.7 多入口并发

职责：UI、IM 渠道、定时任务、API 多个入口触发 run 时的互斥与排队。

必须回答：并发上限与互斥单元（全局单 run？按会话？）？被拒绝的请求是排队还是报错？各入口是否走同一个生命周期管理器？入口间判断依据不一致（轮询延迟窗口）时会怎样？

常见做法：所有入口收敛到同一个 run 管理器（绕过者 = 状态真相分叉）；拒绝要有用户可见的原因；排队消息持久化（内存队列刷新即丢）。

### 4.8 上下文管理

职责：决定什么进入模型的当前调用窗口——压缩、截断、卸载。

必须回答：压缩触发条件与分级策略？压缩后工具调用对齐如何保证（孤儿 tool_call/tool_result 会直接触发 API 错误）？压缩失败/被中断时上下文处于什么状态？对话历史丢失如何防（MAST 高频故障模式之一）【评审】？

常见做法：分级管线（衰减 → 比例检查 → 摘要 → 硬截断）+ 每级后做对齐修复；压缩操作自身要有事务性（失败回退到压缩前，不留半压缩状态）【经验, Howpot 压缩管线 + 2026-07 系统性修复的教训】。

**resume 必须尊重压缩边界**：Claude Agent SDK 在 transcript 里显式记录 compaction 边界，续跑时把压缩后摘要当生效上下文，而不是重放压缩前全量历史（后者要么超限要么让压缩前功尽弃）；压缩后还重新读取指令文件，防止系统级指令被一并冲掉【官方, Claude Code session-storage 文档, 基于检索摘要】。自研 harness 的 resume 若不知道"压缩发生过没有"，两头都是坑。

### 4.9 进程与壳层生命周期

职责：桌面壳、server 子进程、窗口三者的生死管理。

必须回答：壳如何知道 server 就绪/死亡？server 死了壳做什么（重启策略）？窗口关闭时 run 的收尾窗口有多长？二次启动、端口占用、僵尸进程如何处理？

常见做法：supervision 降维（3.4）——健康检查 + 退避重启 + 连续失败呈现可操作的诊断页（有重试按钮，不是死胡同）；退出流程检查活跃 run 并给持久化留时间窗；子进程生命周期绑定父进程（进程组/Job 对象），孤儿清理只是兜底不是主策略。

### 4.10 可观测性与用户感知

职责：让系统状态对开发者和用户都可见、可信。

必须回答：transcript/事件日志记什么、在哪查？后台任务完成用户怎么知道？健康指示器由什么驱动（一次性检查还是持续信号）？错误到达用户的路径是什么（每类错误都要有）？

常见做法：
- **面向沉默失败设计**。生产系统事故复盘给出五类沉默失败（环境怪癖/假设不匹配/错误吞噬与稀释/链式幻觉/操作盲点），其中最长一起持续 60 天未被发现；70% 的沉默失败最终靠人工看输出发现，而非测试或健康检查【预印, arXiv:2606.14589, 单作者非同行评审, 引用需标注局限】。
- **破坏验证（sabotage validation）**：每加一个看门狗/检测器，故意制造它该捕获的违规，验证真的会触发再撤销——"未经验证的检测器与空检测器无法区分"（同上论文发现 67 个空检查跑了数月）【预印, 同上】。
- **声明状态与运行时状态定期对账**：声明的（配置里应有的）和观察到的（实际跑着的）做 diff，先告警后自动收敛【预印, 同上】。
- 完成感知接入系统通知/窗口注意力，不能只靠用户盯着看（Howpot 用户补报的实证缺口）【经验】。

### 4.11 UI 韧性

职责：前端在数据异常、后端异常、导航干预下的存活与恢复。

必须回答：ErrorBoundary 边界画在哪层（爆炸半径）？Retry 语义是什么（清状态重渲染同一份坏数据 = 死循环）？自动恢复上次会话失败时有没有熔断？外链/导航逃逸如何防？本地持久化状态损坏如何容错？

常见做法：分层边界（消息区一层、外壳一层，单条消息渲染容错）；Retry 附带数据重拉；自动恢复加"连续失败回退空白态"熔断；桌面壳外链一律交系统浏览器（拦截 + opener）+ on_navigation 白名单双保险；localStorage 读写全部 try/catch 且带版本号。

### 4.12 运维通道:升级、备份、迁移

职责：低频但高危的自我改变操作。

必须回答：热更新/还原与活跃 run 的互斥？更新后运行的代码与标记的版本是否一致（漂移窗口）？备份对 WAL 模式数据库是否正确（3.2 备份陷阱）？回滚失败的引导路径？跨版本数据迁移的前后校验？

常见做法：运维操作前置 active-run 门槛（run 管理器就在那里，接上即可）；更新成功后强制重启提示而非无限期漂移；备份用 backup API 或 checkpoint 后三文件同拷；每条运维路径都要有失败后的用户引导，不能停在异常栈。

## 五、反模式与注意事项

### 5.1 头号反模式:半途设计（机制只接了一半的线）

最常见也最隐蔽的一类：机制的主路径做了，与其他机制交叉的那一半没做。它不是 bug——每个机制单独看都"正常工作"——所以测试测不出来，只在交叉场景显形。Howpot 摸底一次性抓到五例同构【经验】：

1. SSE 断开不杀后台 run（做了）→ 切回来重新接上流（没做）
2. run 后台跑完落库（做了）→ 完成后让用户感知（没做）
3. 工具改文件实时快照（做了）→ 崩溃时归属消息一起落（没做）
4. F5 恢复拉运行时快照（做了）→ 快照含消息内容、持续更新（没做）
5. 启动失败诊断页（做了）→ 页面上的重试出口（没做）

检测方法就是第四章每个域的"必须回答的问题"清单：新机制上线前，对着已有机制逐个问"交叉时每个状态的归宿"。回答不出 = 线只接了一半。

### 5.2 持久化反模式

- 整个 run 包在一个大事务里：WAL 无法 checkpoint，中途崩溃丢掉全部已完成 step，零增量恢复能力。
- 每个 token delta 一次事务提交:无恢复价值，频繁 fsync 拖垮性能。
- 只在收尾一次性落库：run 期间数据库是盲区，一切中途查询/崩溃/切换都踩空（Howpot P0-1）。
- 备份 WAL 库只拷 `.db` 单文件。
- UI 状态反向修正数据库（真相源倒置）。
- "有 intent 无 result" 的工具调用崩溃后无脑重试——对非幂等副作用等于重复执行。

### 5.3 错误处理反模式

- 空 catch 与错误稀释：多层调用把可操作的错误信息层层剥离成不可用的告警，或直接吞掉【预印, arXiv:2606.14589】。判定标准：影响用户可见行为的 catch 不允许静默。
- 出错分支不发流终结信号 → 客户端永久等待。
- 一次性检查冒充持续状态（启动时查一次健康就永远显示绿灯）。
- Retry 按钮只清本地状态不重拉数据——坏数据死循环。
- 把"编译通过/schema 合法"当"正确"——缺语义层和编排层校验。

### 5.3+ 业界点名的架构反模式

- **软约束冒充硬控制**（违反 T4）：压缩、权限确认、guardrail 做成 prompt 层约束，指望模型自觉遵守——模型不配合就失控。控制机制必须走模型输出之外的确定性路径【预印, arXiv:2606.10106】。
- **resume 无视压缩边界**：断点续跑直接重放全量历史（4.8 详述）。
- **内存态持久化上生产**：MemorySaver 类组件重启全丢且无告警【官方, LangGraph】。
- **不懂底层就上重框架**：出问题分不清是框架封装还是业务逻辑——Anthropic 明确点名的客户常见错误，建议从直接调用底层 API 开始【官方, Building Effective Agents】。
- **任务性质误判**：可枚举的固定流程用自主 agent 实现，为不需要的自主性支付不确定性和成本【官方, 同上】。

### 5.4 恢复与自愈的代价意识

自愈不是免费的：实测多层自愈（重试/回滚/重提示/换工具/缓存）提升成功率 15-25%，但带来 20-40% 延迟开销，且对训练数据外的新故障模式效果有限【预印, arXiv:2606.01416】。两条纪律：恢复过程要对用户可观测（"正在恢复"而非静默变慢）；重试必须有界并带断路器，无界重试比快速失败更伤。

### 5.5 评估与归因的陷阱

- harness 改进非加性、回归难预测（二章末尾数据）→ 改动必须回归测试。
- 故障归因先做三源分流（模型侧/环境侧/harness 侧），再决定在哪层修——把 harness bug 误判为模型幻觉去调 prompt 是常见的方向性浪费【预印, arXiv:2603.29231】。
- 验收用 pass^k（k 次全过）而非单次 pass@1，长程任务尤其如此【预印, 同上】。
- 基础设施故障的 trial 计零分而非丢弃，否则成功率被人为抬高、harness 脆弱性被掩盖【经验, 评测 harness 通行做法】。
- 借用故障分类法要先做适用性判断：MAST 是多智能体分类法，FC2 类（agent 间失调）不适用单 agent；它也明确排除了效率/成本/安全维度，不是故障全集【评审, arXiv:2503.13657】。

---

## 六、案例:用框架复盘 Howpot 摸底

这一章证明第四章框架的解释力：2026-07-09 韧性摸底的全部确认发现，都能落进十二个域，且两个用户报告的 bug 各自横跨三个域——这正是"交叉生效"的实证。

**Bug 1（切回运行中会话空白）横跨三域**：4.2 持久化时机（run 中零落库 → 查询盲区）× 4.3 流式传输（无重连通道）× 4.11 UI 韧性（视图销毁重建只读最终态）。任何单独一个域做好了，这个 bug 都不会以这个形态出现。

**Bug 2（点链接后记录消失）横跨三域**：4.11 UI 韧性（导航逃逸无防护）× 4.2 持久化时机（被打断轮次未落库，制造"丢了"的假象）× 4.1 生命周期（抢占语义不等旧 run 落盘，把假丢变成真丢）。用户行为链（跳走 → 误判 → 重问）恰好把三个缺口串成一次数据丢失。

其余 P0 与高危 P1 的归属：退出强杀 → 4.9 进程生命周期 + 4.4 中断分层；还原覆盖活跃库 → 4.12 运维通道；确认挂起锁死 → 4.5 人机挂起；假停止 → 4.4 中断分层；ErrorBoundary 崩溃循环 → 4.11；后台完成无感知 → 4.10；热更新四连 → 4.12。没有一条发现落在框架外——写教程时可以反过来讲：**先给框架，再用这批发现当习题**。

另一个值得写进教程的元案例：摸底过程本身也遇到了 harness 故障——研究用的后台 workflow 两次静默卡死（网络请求僵死、无心跳、无超时看门狗），靠人工发现异常。诊断手段（对比转录文件 mtime）、处理策略（断路器：两次失败停止重试同一方案，降级为直连补跑）正是 4.10 与 5.4 的现场演示。

## 七、教程组织建议

一个可行的教程骨架（按认知顺序而非本文的参考书顺序）：

1. **开场用事故**：Bug 2 的完整因果链（跳走→假丢→重问→真丢）作为引子——三个"各自正常"的机制如何串成一次数据丢失。事故比定义更能让读者理解"交叉生效"。
2. **给定义**：harness 六元组 + 工程化三判据（每个状态有归宿/每个故障有路径/每个中断可恢复）。
3. **立论点**：23.8pp 与 MAST 干预实验——工程质量是独立变量，值得专门投入。
4. **讲根基**：第三章的五块，每块配一个"单机降维"结论，防止读者照搬分布式重型方案。
5. **过清单**：第四章十二域。教学形态建议做成"设计评审 checklist"——每域列"必须回答的问题"，让读者拿自己的系统当场回答。
6. **讲反模式**：半途设计置顶（配 Howpot 五例），其余按第五章分组。
7. **收尾于流程**：工程化不是一次审计而是持续纪律——新机制上线前过 checklist、检测器上线时做破坏验证、改动跑回归、故障做三源归因并入分类标签库。

写作提醒（来自本文成文过程）：所有定量结论标注来源等级；学界空白（运行时状态持久化几乎没有同行评审研究、中断研究只覆盖用户改需求不覆盖运行时强制中断）本身是教程的价值点，明说"这里业界实践跑在学术前面"。

## 八、来源清单（分级）

**同行评审/大样本实证**
- MAST: Why Do Multi-Agent LLM Systems Fail? — arXiv:2503.13657, NeurIPS 2025 D&B spotlight。三大类 14 模式故障分类法，Kappa 0.88，干预实验 +9.4/+15.6pp。
- WebArena — arXiv:2307.13854。评测 harness 用容器快照冻结环境的可复现范式。

**arXiv 预印本（未同行评审，引用需标注）**
- Harness 六元组综述 — arXiv:2606.20683。harness 形式化定义。
- What makes a harness a harness — arXiv:2606.10106。T1-T4 四要件测试，harness 与 agent 脚本的分界。
- Harness-Bench — arXiv:2605.27922。23.8pp 差异实验，execution alignment 概念。
- Agentic Harness Engineering — arXiv:2604.25850。组件非加性（+11.1pp→+7.3pp）、regression 预测精度 11.8%。
- Self-Healing Agentic Orchestrators — arXiv:2606.01416。语义/编排层故障，自愈 15-25% 收益 vs 20-40% 延迟代价。
- Beyond pass@1 — arXiv:2603.29231。三源方差分解，pass^k。
- 生产 LLM 运行时沉默失败分类 — arXiv:2606.14589。五类沉默失败、破坏验证、67 个空检查。**单作者、单系统、非同行评审**。
- AgentBay — arXiv:2512.04367。介入分类与中断最小状态集。
- InterruptBench — arXiv:2604.00892。中断三分类（Addition/Revision/Retraction），只覆盖用户改需求。
- AgentFail — arXiv:2509.23735。低代码平台三层故障抽象。
- Lingering Authority — arXiv:2606.22504。可撤销权限。

**官方文档/经典工程文献**
- SQLite WAL 官方语义（checkpoint 四模式、备份三文件）
- Martin Fowler: Event Sourcing（martinfowler.com/eaaDev/EventSourcing.html）
- Martin Kleppmann: logs for data infrastructure（2015）
- AWS Prescriptive Guidance: Transactional Outbox Pattern
- Gunnar Morling: On Idempotency Keys
- Kevin Sookocheff: WAL and ARIES
- Temporal 文档: Workflow Execution / Event History（追加式日志、确定性约束、容量限制与 Continue-As-New、signal/query/update）
- Erlang/OTP 文档: Supervisor Behaviour（三种重启策略、MaxR/MaxT 重启强度限制）
- Candea & Fox: Crash-Only Software, HotOS IX 2003（usenix.org 原文）
- Google SRE Book: Monitoring Distributed Systems（三支柱、golden signals）
- InfoQ 2025-09: Temporal and OpenAI Launch AI Agent Durability（durable execution 进入 agent 领域的标志事件）
- LangGraph 文档: interrupts / checkpointer
- WHATWG: Server-Sent Events 规范（Last-Event-ID）
- OpenAI Assistants API: run lifecycle 与 thread 锁语义（部分细节来自检索摘要与社区故障报告归纳，未逐字核对官方原文）
- Anthropic: Building Effective Agents（workflow vs agent 决策、ACI 设计四实践）
- Claude Code / Agent SDK: session-storage（resume 与 compaction 边界；基于检索摘要）
- MCP lifecycle 规范（能力协商/超时清理；本次抓取 404，要点基于模型知识，引用前需核对最新版）

**一手实证**
- Howpot Chat 韧性摸底报告 — `howpot-chat/docs/resilience-audit-2026-07-09.md`（9 路审计、35 项对抗验证、5 条 P0）

**成文说明**：本文材料来自 3 份完整研究笔记（状态持久化、学术故障研究 ×2）、1 份完整业界实践笔记，以及 2 份从中途卡死的研究 agent 转录中抢救的官方文档摘录（Temporal/OTP/crash-only/SRE，均为 WebFetch 官方源的直接产物）。《Release It!》与失败模式三分等少量段落基于模型知识，已逐处标注【经验】——教程正式引用前建议核对原文。
