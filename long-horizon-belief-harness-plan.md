# 长程信念 Harness:要点整理与行动计划

> 2026-07-13,基于三轮讨论 + GPT 方案评估。目标:从"聊迷茫了"回到一条可执行的最小路径。

## 〇、一句话结论

GPT 的方案方向正确、结构完整,但对单人两周 MVP 来说太重。建议:砍到四个模块;把 **Git 历史挖掘**提到第一优先——它同时给你评测基准、记忆内容和未来训练数据,且反馈即时、完全离线;**暂不微调 Qwen**,微调是第 4 步的事。

---

## 一、讨论至今的核心结论(定盘)

1. **Long horizon 的定义是反馈密度,不是时长**:从一个决策到"能评判该决策的信号"之间隔了多少个其他决策。核心难度是 credit assignment。
2. **"信念"的计算本质 = 价值函数 + 不确定下的持续投入承诺**,不是 100% 相信。confidence(多大概率为真)/ commitment(愿投多少资源验证)/ invariants(不可突破的边界)必须分开。
3. **长期主义与可纠正性天然冲突**,解法是可证伪:falsifier + review 时点 + 独立 verifier,而不是在 system prompt 里写"无论如何都要坚持"。
4. **"绕远"无法编码为策略,只能定义功能**:信息增益、能力建设、期权价值、风险消减、可复用资产。绕远行为要靠价值判断涌现(AlphaGo 弃子)。
5. **涌现绕远的配方**:足够长的 horizon + 只在终点结算的奖励 + 海量廉价试错。现实世界缺第三样,用"可校准的结构化预测"替代模拟器。
6. **复用要分层**:抽象原则可跨环境复用;具体的执着要在新环境重新挣得,否则是对旧环境过拟合的信念。
7. **时间抽象可能比"熬过百万步"更关键**:里程碑表征把客观长任务变成主观短任务,无反馈区间的"反馈"来自自己的世界模型("我在第三阶段,符合预期")。
8. **不动权重也能学**:episodic → semantic → procedural 的记忆巩固(带晋级条件)是第一阶段的持续学习方式(Voyager、Reflexion 已验证)。

---

## 二、对 GPT 方案的评估

### 2.1 值得保留(核心正确)

- Commitment 对象化、版本化、可审计,confidence/commitment/invariants 三分。
- 绕路的功能性定义 + 准入规则(明确假设、资源上限、kill condition、复查时间)。
- 不做单一总 reward,用"硬约束 → Pareto 筛选 → 预算组合"。
- 独立 verifier 只认行为证据(付费、重复使用、日志),"用户说挺好"只是弱证据。
- 三层记忆 + lesson 晋级条件(≥3 次、≥2 场景、无强反例、改善保留集)。
- "放弃/暂停/切换"永远是显式候选——否则系统天然沉没成本偏向。
- 内在动机设限额(noisy-TV 和 empowerment 风险是真实的),奖励"预测误差持续下降",不奖励原始新奇度。
- 核心待验证命题的表述本身很好:**同模型同预算下,结构化信念账本 + 有限绕路 + 离线巩固,能否比普通 Agent 更好地穿越误导性短期反馈,同时保持可纠正性。**

### 2.2 四个被轻描淡写的风险

1. **工程量与 MVP 不匹配**。六模块 + 六张表 + 三情景模拟 + 五组对照消融,单人两周做不完。最大风险:两周全花在管道上,零轮真实实验。
2. **计划期自评分回潜**。行动候选里的 `information_gain: 0.8` 是 LLM 自己报的数——这正是独立 verifier 要解决的自评问题,在 planner 环节又溜回来了,数字必然是编造的。缓解:把这些数当作**待校准的预测**记录进 Brier 循环;行动选择主要靠配额 + 硬约束,不靠编造的量值排序。
3. **falsifier 的执行权**。证伪条件触发时,若由同一个 LLM"重新评估",必然移动球门。falsifier 判定必须机械化:到 review 日期,规则对照已记录证据自动判定;LLM 只能提出申诉(生成新 Commitment),不能否决判定。
4. **慢实验混淆变量**。8–12 周产品实验里,商业运气和 harness 质量分不开。它只用于校准世界模型,不用于评价算法——GPT 自己也写了,但排期要更狠:**第一个月只做快环境**。

另补一条 GPT 完全没提的:**harness 自身开销记账**。如果 Belief Harness 每轮多花 3 倍 token,对照组 ReAct 必须给 3 倍预算,否则结论无效。

### 2.3 v0.1 砍到四个模块

保留:**Belief Ledger / Planner(五类候选+配额)/ Executor+Verifier / Consolidator**。

- 砍 Scenario Simulator(三情景生成)→ 降级为"每个行动记录一行预测 + 概率",先积累校准数据;等有 50+ 条 forecast 记录再考虑结构化情景。LLM 编的故事在没有校准历史之前没有信息量。
- 砍 反主流路线预算、多假设组合 → v0.2。
- 砍 消融矩阵 → 第一轮只跑两组:ReAct(同预算)vs Belief Harness。

---

## 三、Git 历史 + 本地 Qwen:正确的路径

### 3.1 为什么不先微调

- 原始 commit 不是指令数据。直接 SFT/LoRA 只会学到代码风格和 commit message 口吻,学不到长程判断,还有灾难性遗忘风险。
- 我们真正要的是 **"决策 → 延迟后果"配对**——这才是 long-horizon 数据。先提取,再谈训练。

### 3.2 四级阶梯(每级独立有产出)

**第 1 级:挖掘(第一周就能做)**
用 PyDriller / git log 把仓库变成结构化事件流,重点提取:

- revert 对:被撤销的 commit 及时间间隔 = "事后被证伪的决策";
- SZZ 算法:定位 bug 引入 commit ↔ 修复 commit = "决策 → 延迟惩罚";
- 高 churn 文件/模块 = 维护成本高的架构决策;
- 长寿代码(一次写对、极少改动)= 好决策;
- hotfix 链、TODO 的解决时长。

**第 2 级:评测基准(第二周)**
按时间切分:以某日期前的历史为上下文,让模型预测"这个 commit 未来会不会被 revert / 引发 bugfix",用 Brier score 打分。这直接测"世界模型校准"——整个讨论里最缺的那块能力,而且离线、反馈即时,是现成的自有数据快环境。

**第 3 级:记忆注入**
把挖出的规律写成 semantic memory(带 supporting episodes、反例、适用范围、失效条件),接入 Belief Ledger;对比"有/无 Git 记忆"的 agent 在该仓库任务上的完成率。

**第 4 级:训练(仅当第 2、3 级出现信号)**
用结果标注的决策对做 DPO / LoRA adapter:被 revert 的决策为负样本,长寿决策为正样本。这正好落在 GPT 说的"第二阶段"条件内。

### 3.3 本地 Qwen 27B 的定位

- 跑挖掘、批量摘要、打标:完全够,便宜是核心优势。
- 当 planner:偏弱,实验结果会低估强模型上限。建议 harness 开发期用本地模型(迭代快、零成本),关键对照实验跑两档模型(本地 + 一个强 API 模型),看结论是否随模型能力保持——**如果 harness 只在强模型上有效,这本身就是重要发现**。
- 微调可行性:27B 用 QLoRA 约需 48GB 级显存;但按阶梯这是第 4 级的事,现在不用纠结。

---

## 四、修订后的两周排期

**第一周**

- Git 挖掘管道 + revert/SZZ 数据集(2–3 天);
- Belief Ledger(SQLite:goal / belief / evidence / forecast / episode 五张表,skill 表推迟);
- 五类行动候选 + 配额选择器,"放弃"为常驻候选;
- 每个行动记录预测(expected_observation + 概率)。

**第二周**

- Verifier:机械规则优先,LLM 判定仅限无规则可用处;falsifier 到期机械触发;
- 每日 consolidation,lesson 晋级条件照 GPT 的四条;
- 用 Git 预测基准跑第一轮 Brier 校准;
- 搭 misleading-unit-test 快环境(GPT 第八节的 50–100 步项目),跑 ReAct vs Belief Harness 两组。

**不做清单**:照抄 GPT 的——不动基座权重、不让 agent 改硬约束、不用 LLM 自评代替 verifier、不无限自主运行、不把记忆全塞 context、不用单一总 reward。全部同意。

---

## 五、两周结束时的成功判据

回答三个问题:

1. Brier 分数随 episode 数下降吗?(世界模型在校准)
2. 快环境里,Belief Harness 相对**同 token 预算**的 ReAct,端到端成功率有差异吗?
3. 是否发生过至少一次:falsifier 机械触发 → 路线被纠正 → 没有球门移动?

三个都是"否" → GPT 的核心命题第一轮被证伪,回来重新讨论方向;任何一个"是" → 值得继续投入,再考虑消融和第二阶段训练。
